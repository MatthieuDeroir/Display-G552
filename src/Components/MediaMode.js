// src/components/MediaMode.js
import React from 'react';

const MediaMode = ({ mediaState }) => {
    // Utilisez les données mediaState pour afficher votre UI
    return <div>Media Mode</div>;
};

export default MediaMode;
