// src/components/ScoreMode.js
import React from 'react';

const ScoreMode = ({ gameState }) => {
    // Utilisez les données gameState pour afficher votre UI
    return <div>Score Mode</div>;
};

export default ScoreMode;
